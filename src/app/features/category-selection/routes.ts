import { Routes } from '@angular/router';
import { CategorySelectionComponent } from './category-selection.component';

export const CATEGORY_SELECTION_ROUTES: Routes = [
  { path: '', component: CategorySelectionComponent }
];
