import { Routes } from '@angular/router';
import { StudentRegistrationComponent } from './student-registration.component';

export const STUDENT_REGISTRATION_ROUTES: Routes = [
  { path: '', component: StudentRegistrationComponent }
];
